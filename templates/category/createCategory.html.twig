{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-3">
        <div class="card">
            <h5 class="card-header">Créer une Category</h5>
            <div class="card-body">
                {{ form_start(form,{'attr': {'novalidate' : 'novalidate'}} ) }}
                <div class="mb-3">
                    {{ form_label(form.title, 'Titre') }}
                    {{ form_widget(form.title) }}
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}